
/**
 * File types
 * - CSV
 * - OFV
 * - JSON
 * - XML
 * @description Enum for the different file types
 */
export enum FileTypes {
    CSV = 'csv',
    OFV = 'ofv',
    JSON = 'json',
    XML = 'xml',
}
