import * as CategoryActions from '../actions/categories.actions';
import { initialState } from '../actions/categories.actions';


// export const categoriesReducer = createReducer(
//   initialState,
//   on(CategoryActions.resetCategories, state => ({...state, categories: []})),
//   on(CategoryActions.setCategories, (state, newState) => ({...state, categories: newState.categories }))
// );
