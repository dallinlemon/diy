import * as BudgetActions from '../actions/budget-menu.actions';
import { initialState } from '../actions/budget-menu.actions';


// export const budgetMenuReducer = createReducer(
//   initialState,
//   on(BudgetActions.resetBudgetMenu, state => ({...state, menu: initialState.menu})),
//   on(BudgetActions.setBudgetMenu, (state, newState) => ({...state, menu: newState.menu }))
// );
